<div class="container">
	<div class="row">
		<div class="col-xs-3 sidebar">

<%= render 'users/profile', user: current_user %>
<!--メモ　右側の指定に注意。そこを間違えるとエラーが発生して表示されない -->
<%= render 'books/newform', book: @book %>
		</div>
		<div class="col-xs-9">

			<%= render 'users/userindex',users: @users %>
		</div>
	</div>
</div>